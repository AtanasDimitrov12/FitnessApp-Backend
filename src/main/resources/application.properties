# Application Name
spring.application.name=fitness-app

# CORS Header for local development
corsheader=http://localhost:5173

# Cloudinary Configuration
cloudinary.cloud_name=dgovaqahy
cloudinary.api_key=121874441832724
cloudinary.api_secret=JEK2u4T9ix-MI64_inc7v34AtIM

# H2 Database Configuration
spring.datasource.url=jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password= # leave blank for H2
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console

# Enable Hibernate to automatically create/update database tables
spring.jpa.hibernate.ddl-auto=update

# Uncomment this if you encounter circular dependencies (temporary fix, not recommended for production)
# spring.main.allow-circular-references=true
